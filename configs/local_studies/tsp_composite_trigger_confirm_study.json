{
  "study_name": "tsp_composite_trigger_confirm_study",
  "description": "Confirm the best fired TSP composite trigger against the static baseline and the existing simple threshold trigger.",
  "problem": "tsp",
  "base_preset": "tsp_small",
  "shared_overrides": {
    "population_size": 40,
    "generations": 60,
    "log_every": 1,
    "algorithm_options": {
      "refresh_fraction": 0.05,
      "adaptation_cooldown": 5,
      "stagnation_window": 10,
      "stagnation_boost_factor": 1.5,
      "improvement_epsilon": 0.5,
      "diversity_slope_threshold": 0.0
    }
  },
  "sweep": {
    "algorithm_options.adaptive_policy": [
      "none",
      "low_diversity_injection",
      "composite_trigger_v2"
    ],
    "algorithm_options.diversity_threshold": [0.12]
  },
  "seeds": [401, 402, 403, 404, 405, 406, 407, 408, 409, 410],
  "budget_ceiling": 2480,
  "primary_metric": "best_route_distance",
  "plotting": {
    "history_metric": "best_route_distance"
  },
  "runtime_budget_note": "10-seed confirm for TSP trigger redesign. Composite should beat both the static baseline and the simple threshold trigger to survive."
}
