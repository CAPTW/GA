{
  "study_name": "zdt1_diversity_threshold_study",
  "description": "Focused ZDT1 threshold sweep for low-diversity injection while keeping the NSGA-II budget matched.",
  "problem": "zdt1",
  "base_preset": "zdt1_small",
  "shared_overrides": {
    "population_size": 60,
    "generations": 80,
    "log_every": 1,
    "algorithm_options": {
      "adaptive_policy": "low_diversity_injection",
      "refresh_fraction": 0.2,
      "adaptation_cooldown": 4,
      "stagnation_boost_factor": 1.5
    }
  },
  "sweep": {
    "algorithm_options.diversity_threshold": [0.35, 0.45, 0.55, 0.7]
  },
  "seeds": [231, 232, 233, 234],
  "budget_ceiling": 14520,
  "primary_metric": "hypervolume",
  "plotting": {
    "history_metric": "hypervolume"
  },
  "runtime_budget_note": "Stage-1 ZDT1 threshold sweep around the current low-diversity signal region."
}
