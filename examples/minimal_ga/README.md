# Minimal Pure GA

Run:

```bash
ga-lab-demo pure-ga --output-root outputs/demo
```

Look at:

- `outputs/demo/pure_ga/demo_result.json`
- the generated `summary.json` under the reported output directory

What to read:

- `best_fitness`
- `stop_reason`
- `convergence_generation`
